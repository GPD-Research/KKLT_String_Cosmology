[package]
name = "kklt_string_cosmology"
version = "0.1.0"
edition = "2021"
description = "KKLT scalar potential utilities for physics-ide"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
